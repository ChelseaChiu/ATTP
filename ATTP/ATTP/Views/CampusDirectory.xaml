<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ATTP.Views.CampusDirectory">
    <ContentPage.Content>
        <StackLayout>
            <StackPanel>
            <!--<control:CustomPicker x:Name="CRNDAYPicker"  Title="Select the timetable day " Grid.Row="3" ItemsSource="{Binding CRNCourseDays}" ItemDisplayBinding="{Binding CRNCourseDayName}" Image="ic_arrow_drop_down" />-->
            <Picker x:Name="campusPicker" Title="Pick the Campus" />

            <Picker x:Name="imagePicker" Title="Pick the floor to view" ItemsSource="{Binding FloorList}" SelectedIndexChanged="imagePicker_SelectedIndexChanged"/>

            <!-- <Picker x:Name="FloorPicker" Source="" -->
            <Image x:Name="floorImg" Source="Assets/CampusImages/SouthAustralia/AllCampuses.png" />
            </StackPanel>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>